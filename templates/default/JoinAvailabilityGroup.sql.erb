<% @nodes.each do |node| %>
:Connect <%= node.hostname %>

IF NOT EXISTS(SELECT * FROM sys.availability_groups WHERE name='<%= @name %>')
BEGIN
    ALTER AVAILABILITY GROUP [<%= @name %>] JOIN;
END

GO
<% end %>